#!/bin/bash

#7.5
printf "Setting default group for root account:\n"
DGROUP=$(grep "^root:" /etc/passwd | cut -f4 -d:)
if [ "$DGROUP" -eq 0 ]; then
	printf "\e[32mNo remediation is needed\e[0m\n"
else
	usermod -g 0 root
	printf "\e[32mRemediation has been completed\e[0m\n"
fi
